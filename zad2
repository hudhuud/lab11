#Задание 2. Matrix7. Дана матрица размера M ? N и целое число K (1 ? K ? M). Вывести элементы K-й строки данной матрицы.
#Шарапова Ангира 21 ис 21

m, n, k = [int(x) for x in input().split()] #ввод столбца, строки и элемента
lst = []
count = 0
for i in range(m):
    s = []
    for j in range(n):
        s.append(count)
        count += 1
    lst.append(s)
print(lst[k-1])

